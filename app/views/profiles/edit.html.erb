<% content_for :title, "Chỉnh sửa hồ sơ — RoadMate" %>

<div class="min-h-screen bg-gray-50 py-8 px-4">
  <div class="max-w-lg mx-auto">
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-gray-900">Hoàn thiện hồ sơ</h1>
      <p class="text-gray-500 mt-1 text-sm">Hồ sơ đầy đủ giúp tăng tỉ lệ ghép chuyến.</p>
    </div>

    <%= render "form", user: @user %>
  </div>
</div>

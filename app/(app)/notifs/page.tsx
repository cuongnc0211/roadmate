import { PageStub } from "@/components/shell/page-stub";

export const metadata = { title: "Thông báo" };

export default function NotifsPage() {
  return (
    <PageStub
      title="Thông báo"
      note="Thông báo in-app (request mới, được duyệt/từ chối, huỷ/rút) — Phase 05."
    />
  );
}
